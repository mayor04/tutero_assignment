import 'package:flutter/material.dart';

class PausedView extends StatelessWidget {
  const PausedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
